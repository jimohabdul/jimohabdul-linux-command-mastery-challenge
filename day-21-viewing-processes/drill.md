# Practice Drill: Viewing Processes

## Practice Sequence Executed:
1. **PID Discovery**: Located target process PIDs using `pgrep` and `ps aux | grep`.
2. **Live Monitoring**: Inspected active workloads and metrics interactively inside `top`.
3. **Lineage Mapping**: Traced process relationships using `pstree`.
4. **Port Inspection**: Identified network services bound to port connections using `sudo lsof -i`.
