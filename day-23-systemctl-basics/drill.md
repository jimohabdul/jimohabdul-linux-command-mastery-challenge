# Practice Drill: Init Systems & systemctl Basics

## Practice Sequence Executed:
1. **Service Control**: Selected a target service (e.g., `ssh` or `nginx`), stopped it, and confirmed its inactive state.
2. **Lifecycle Management**: Restarted the service and enabled it to auto-start at boot using combined commands.
3. **State Verification**: Queried `is-active` and `is-enabled` to audit the final configuration.
