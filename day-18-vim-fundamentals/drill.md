# Practice Drill: Vim Fundamentals

## Practice Sequence Executed:
1. **File Creation**: Opened a new file (`vim test.txt`).
2. **Text Insertion**: Entered Insert mode (`i`) and typed three lines of text, then returned to Command mode (`Esc`).
3. **Saving**: Wrote the file changes to disk using `:w`.
4. **Line Editing & Undo**: Reopened the file, deleted the middle line (`dd`), successfully restored it using undo (`u`), and safely saved and exited (`:wq`).
